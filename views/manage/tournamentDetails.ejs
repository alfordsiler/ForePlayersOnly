<div class="remove">

<h3>Here are the teams entered into "<%= tournament.name %>":</h3>

<% teams.forEach(function(team){ %>
<ol>
  <li><h4><a href="/manage/teams/<%= team.id %>"><%= team.name %></a> <a style="font-size: 8px; padding: 2px; margin-bottom: 5px;" href="/manage/teams/<%= team.id %>" class="btn btn-danger glyphicon glyphicon-trash delete-btn-users"></a></h4></li>
</ol>
<% }); %> 

<form action="/manage/tournaments/<%= tournament.id %>" method="POST">
  <input type="hidden" value="<%= allTeams.id %>">
  <label for="teams">Register a Team to play in <%= tournament.name %>:</label>
  <select name="team">
    <% allTeams.forEach(function(team){ %>
    <option value="<%= team.id %>">
      <%= team.name %>
      <% }); %>
    </select>

    <input type="submit" class="btn btn-success" value="Add" style="font-size: 12px; height: 27px;">
  </form>
  <br>

  <a href="/manage/tournaments">&larr; Back to Your Tournaments</a>

</div>