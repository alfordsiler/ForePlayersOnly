<div class="remove">

<h3>Manage Your Teams</h3>
<h5>Click on a team to view players</h5>

<hr>

<% if(teams) { %>

  <% teams.forEach(function(team){ %>
    <h3><a href="/manage/teams/<%= team.id %>"><%= team.name %></a></h3>
    <p>Created On: <%= team.createdAt.toDateString() %></p>
    <a href="/manage/teams/<%= team.id %>/edit" class="manage-btn btn btn-info glyphicon glyphicon-edit"></a>
    <a href="/manage/teams/<%= team.id %>" class="manage-btn btn btn-danger glyphicon glyphicon-trash delete-btn-teams"></a>
    <hr>
  <% }); %> 

<% } else { %>

  <h3>You have not created any teams.</h3>

<% }; %>

<a href="/profile">&larr; Back to Manage Teams/Tournaments</a>

</div>